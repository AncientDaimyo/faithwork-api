{% extends 'base.html.twig' %}

{% block title %}FAITHWORK
{% endblock %}

{% block body %}
<div {{ vue_component('HeaderCommon') }}></div>

<div class="cart-checkout">
    <div class="cart-page">

        
    </div>
    <div v-if="showImage" class="product-card-add-to-cart">
        <button v-on:click="addToCart">Очистить корзину</button>
    </div>

    <div class="checkout-block" {{ vue_component('Checkout') }}></div>

</div>

<div {{ vue_component('FooterSecond') }}></div>
{% endblock %}